server {
    listen       8080;
    server_name  localhost;

    # Serve from the html subfolder
    root   /usr/share/nginx/html/html;
    index  index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
